---
description: SafeMeet BBB local development deployment versus CI Debian package publication
globs: "{deploy.sh,deploy.env.example,DEPLOY-FA.md,SAFEMEET-BBB-INSTALL-REPO.md,.github/workflows/**,.github/scripts/**,scripts/safemeet-*,build/**}"
alwaysApply: false
---

# Deploy and Package Pipeline

There are two distinct workflows:

1. Local/staging iteration: `./deploy.sh --only html5` syncs/builds against a configured BBB server using gitignored `.deploy.env`.
2. Branch CI: `.github/workflows/safemeet-ci-cd.yml` builds selected Debian packages and publishes the apt repository. It does not rsync source or run `deploy.sh` on the old BBB server.

- Treat `SAFEMEET-BBB-INSTALL-REPO.md` and the active workflow as CI sources of truth.
- Never commit `.deploy.env`, known-host files, SSH keys, API keys, or server secrets.
- UI-only local deploy should remain `--only html5`; do not rebuild core services without a dependency reason.
- A package/workflow change must identify affected Debian package names and publish/install verification.
- GraphQL schema, Akka, recording steps, and settings changes need explicit service/migration/restart notes.
- After UI deployment, hard-refresh and create a new meeting to avoid cached settings/assets.
- Inspect production read-only before diagnosing production-only issues; mutations require task authorization.
